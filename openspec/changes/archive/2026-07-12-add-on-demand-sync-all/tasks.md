# Tasks

- [x] 1. RepositoryUseCases.sync_all(organization?) reusing trigger_sync (skip running, count)
- [x] 2. REST POST /repos/sync-all (admin, optional organization)
- [x] 3. Web: Sync-now per org on the Connections org panel (VM + API)
- [x] 4. Tests: use case (counts, skip running, org filter), endpoint, VM
