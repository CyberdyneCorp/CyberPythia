# add-code-context-indexing

Phase 3: capture authorized source-code content, chunk it by symbol (functions/classes/modules), embed chunks for semantic code search, and expose code tools over REST + MCP — gated by the code_context/full_context indexing modes and the existing security controls
