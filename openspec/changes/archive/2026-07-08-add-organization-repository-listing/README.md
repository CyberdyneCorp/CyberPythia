# add-organization-repository-listing

Organization filtering for repositories: an `organization` query param on GET /api/v1/repos, two
MCP tools (mnemosyne_list_organizations + mnemosyne_list_organization_repositories), and an
organization filter dropdown on the Repositories dashboard. Read-only, additive; no data model.
