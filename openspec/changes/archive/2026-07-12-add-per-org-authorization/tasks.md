# Tasks

- [x] 1. CallerIdentity.allowed_organizations(product_key) + tests
- [x] 2. Request-scoped allowed_orgs contextvar module (set/get/is_org_allowed)
- [x] 3. Repository store (Postgres + FakeRepositoryPort) filter get/get_by_full_name/list_all
- [x] 4. Set the scope in REST entitled-caller dependency + MCP auth()
- [x] 5. Tests: identity parsing, endpoint 404/filtered for scoped caller, org rollup scoped, MCP scoped, worker unrestricted
