# add-bulk-repository-selection

Bulk enable/disable indexing: POST /api/v1/repos/selection (admin, one batched transaction) plus
Enable-all / Disable-all controls on the Repositories dashboard that act on the current
organization/text filter. No data model change.
